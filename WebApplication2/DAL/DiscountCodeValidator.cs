namespace OnlineWalmart.DAL
{
    public class DiscountCodeValidator
    {
        //public bool Validate(string code, DiscountCode[] validCodes, Order orderToDiscount)
        //{
        //    var discountCode = validCodes.FirstOrDefault(c => c.Code == code);

        //    if (discountCode == null)
        //        return false;
        //    else
        //    {
        //        orderToDiscount.Discount = discountCode.Discount;
        //        orderToDiscount.TotalSavings = discountCode.Discount * orderToDiscount.TotalPrice / 100.0;
        //        orderToDiscount.TotalPrice *= 1 - (discountCode.Discount / 100.0);
        //        return true;
        //    }
        //}
    }
}
